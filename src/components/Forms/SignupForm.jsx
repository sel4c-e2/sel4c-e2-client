import { DataContext } from '@/context/DataContext';

import style from '@/assets/styles/Form.module.css';

export default function SignupForm() {
    return (
        <form action="" className={`${style.form} ${style.signup}`}>
            <div className=''></div>
        </form>
    );
}
